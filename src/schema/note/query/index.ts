import GetFolders from "./get-folders";
import GetNote from "./get-note";
import GetNotes from "./get-notes";

const NoteQueryFields = {
  getFolders: GetFolders,
  getNotes: GetNotes,
  getNote: GetNote,
};

export default NoteQueryFields;
